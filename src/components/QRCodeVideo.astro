---
/**
 * 数字化配套资源兼容外壳 (QRCodeVideo -> DigitalResource)
 * ----------------------------------------------------------------------------
 * 保持对既有 75+ 篇历史 MDX 文件引用的无缝向后兼容，统一委托给全新学术规范组件 DigitalResource。
 * ----------------------------------------------------------------------------
 */
import DigitalResource from './DigitalResource.astro';

interface Props {
  id?: string;
  title?: string;
  url?: string;
  type?: 'auto' | 'video' | 'courseware' | 'demo' | 'resource';
}
---

<DigitalResource {...Astro.props} />
