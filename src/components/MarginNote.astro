---
import SideNote from './SideNote.astro';

interface Props {
  title?: string;
  type?: 'note' | 'think' | 'caution' | 'analysis' | 'geom';
  side?: 'right' | 'left';
  inline?: boolean;
  class?: string;
  [key: string]: any;
}
---

<SideNote {...Astro.props}>
  <slot />
</SideNote>
