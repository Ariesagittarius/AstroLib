interface:
  display_name: "导入新书"
  short_description: "把 task/ 下的 MinerU 新书按既定流程转换为站点 MDX 并注册部署"
  default_prompt: "Use $import-book to import a new MinerU textbook from task/ into the my-astro-site docs site."
