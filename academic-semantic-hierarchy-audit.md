# AstroLib Academic Semantic Hierarchy 重构审计与交付报告

## 一、当前问题审计

在重构前，AstroLib 阅读器中的学术语义块（例题、解答、证明、定理等）存在以下视觉与排版问题：

1. **层级定位倒置与 SaaS 组件化感**：
   - 「例 4.1」作为学术层级标题，未与正文形成清晰锚定边界，而是依靠弱化的文本段落散落呈现。
   - 「解」或「证明」被实现为典型的 SaaS 折叠组件，前置粗糙箭头（`▸ 解`），展开后箭头呈高亮蓝色（`var(--sl-color-text-accent)`），破坏了严肃教材的纯正墨色韵律。
2. **版面碎裂与多余装饰**：
   - 解答正文区域使用了虚线左侧边框（`border-inline-start: 1px dashed var(--sl-color-hairline)`），呈现为 Web 评论引用块质感，而非教材正文的连续段落。
   - 块内容区域存在多余的缩进内边距（`padding-left: 0.5rem` / 移动端 `padding: 0.875rem`），破坏了与上方标题和发丝线的自然垂向对齐。
   - 例题与例题之间缺乏克制的学术分隔线或结构边界，导致读者快速浏览时难以迅速辨别例题起止。

---

## 二、Academic Semantic Hierarchy 设计

确立以「学术层级重于 UI 容器（Semantic hierarchy over UI containers）」为核心原则的统一树状层级：

```text
H1（章标题）
│
└── H2（节标题，如 4.2 定积分在几何中的应用举例）
    │
    ├── 例 4.1（学术对象入口 · 650 SemiBold + 发丝线下划线）
    │   ├── 题目正文（零缩进连续阅读流，标准教材墨色与字阶）
    │   └── 解（答案语义标记 · 内联发丝线 + 后置克制交互提示符 ›）
    │       └── 解答正文（连续纯文本流，结尾右对齐 Q.E.D. ∎）
    │
    ├── 例 4.2
    │   ├── 题目正文
    │   └── 解
    │       └── 解答正文
    │
    └── 定理 2.1（知识/理论对象入口 · 650 SemiBold + 发丝线下划线）
        ├── 定理陈述正文
        └── 证明（数学证明标记 · 内联发丝线 + 后置提示符 › / ⌄）
            └── 证明正文（严密推导流，结尾右对齐 Q.E.D. ∎）
```

**统一定律**：
- 「例 4.1」「定理 2.1」「定义 2.1」为同级学术对象骨架，以 Typography weight (650) 与低对比度 1px 发丝线（hairline）锚定起始边界。
- 「解」「证明」为二级语义推导标记，采用内联发丝下划线与后置单字符提示符（`›` 折叠 / `⌄` 展开），绝不喧宾夺主。
- 整个正文流恢复为正统大学数学教材的印刷体连续文本，零卡片容器背景、零卡片阴影、零彩色圆角边框。

---

## 三、实际修改清单

本任务严格遵循边界约束，只调整学术语义块与交互样式的呈现，未改变任何 HTML 结构、路由或业务逻辑：

1. **核心主题层 (`src/styles/vitepress-theme.css`)**：
   - 重构第 7 节「正文数学对象与模块」：
     - `.card-header, .fallback-header`：新增 `border-bottom: 1px solid var(--sl-color-hairline)` 发丝下划线，外边距与内边距调整为精确学术节奏（`padding-bottom: 0.35rem; margin-bottom: 0.85rem;`）。
     - `.card-body, .fallback-content`：彻底归零左缩进（`padding: 0 !important;`），使公式与段落首字与发丝线起点严丝合缝对齐。
     - `.example-card, .knowledge-card, .method-card, .variant-card, .summary-card, .exercise-card, .fallback-block`：重设垂直间距为 `2.25rem 0 1.5rem`，建立章节内连续对象的呼吸韵律。
     - `.solution-details summary`：
       - 隐藏前置粗箭头（`summary::before { display: none !important; }`）。
       - 引入后置克制提示符（`summary::after { content: '›'; ... }`，展开时顺滑旋转 90 度 `transform: rotate(90deg)`，呈轻量向下箭头 `⌄`）。
       - 改为内联发丝线（`display: inline-flex; border-bottom: 1.5px solid var(--sl-color-hairline);`）。
       - 去除任何蓝色点击/悬停高亮，悬停仅为极轻墨色加深（`border-bottom-color: var(--sl-color-gray-3)`）。
     - `.solution-details .solution-content`：
       - 彻底删除左侧虚线边框（`border: none !important;`）。
       - 消除多余内边距（`padding: 0.15rem 0 0.5rem 0 !important; margin: 0 !important;`），融入自然正文。
2. **移动端响应式规范 (`src/styles/custom.css`)**：
   - 清理 `@media (max-width: 48rem)` 中遗留的卡片式内边距（如 `.card-body { padding: 0.875rem; }`、`.solution-content { padding: 0.875rem; }`），替换为学术流式零内边距排版，释放移动端宝贵水平阅读空间。
3. **组件自包含样式对齐 (`src/components/*.astro`)**：
   - 同步更新了 `Solution.astro`, `Example.astro`, `Knowledge.astro`, `Method.astro`, `Variant.astro`, `Summary.astro`, `Exercise.astro`, `Block.astro` 的 scoped `<style>`，确保在任何环境（包括非 VitePress 主题或打印视图）下均保持同等水准的学术语义层级。

---

## 四、下划线 / 分隔线方案设计

在探索过程中，重点解决了“发丝线不足导致碎片化”与“发丝线过多导致表格化”的平衡问题：

1. **对象标题线（Full-Width Hairline Divider）**：
   - 应用于：`例 4.1`、`定理 2.1`、`定义 2.1` 等学术单元的头部。
   - 属性：`1px solid var(--sl-color-hairline)`（亮色模式下为 `#e8eaed`，暗色模式下为 `#28282c`）。
   - 作用：宣告上一个学术单元结束、新例题/定理开始，形成视线行进时的可靠路标。
2. **解答/证明内联下划线（Inline Hairline Rule）**：
   - 应用于：「解 ›」「证明 ›」标记。
   - 属性：`1.5px solid var(--sl-color-hairline)`，作用于 `display: inline-flex` 的 summary 底部。
   - 作用：清晰界定“解答”作为交互标记的范围，仅包裹字词本身与后置箭头，不在页面横向划开整道长线，避免与上方的例题标题线产生“线条密集排版（Ruled Notebook）”的压迫感。
3. **严禁使用的线条反模式**：
   - ❌ 禁用虚线边框（Dashed border）。
   - ❌ 禁用厚度 $\ge 2\text{px}$ 的彩色高对比度粗线。
   - ❌ 禁用四面包围的卡片边框。

---

## 五、A/B 视觉验证对比

通过在开发服务器上针对核心测试页面 `3.4 定积分的应用`（例 4.1、4.2、4.3）以及 `1.2 数列的极限`（定理 2.1 唯一性与证明）进行无头 Chrome 精确截图对比：

| 评价维度 | 方案 A（原有 Baseline） | 方案 B（全通栏发丝线） | 方案 C（最终极简语义层级） |
| :--- | :--- | :--- | :--- |
| **视觉主体** | UI Accordion / 悬浮组件 | 机械全宽线分割 | **教材连续正文流** |
| **例题标题表现** | 裸粗体文字，无明确起始界限 | 100% 宽度发丝线下划线 | **100% 宽度极低对比度发丝线下划线** |
| **“解”/“证明”标记** | 前置 SaaS 箭头 `▸ 解`，点击转蓝 | 前置无箭头，下挂 100% 宽长分割线 | **后置克制提示符 `解 ›` / `解 ⌄`，内联紧凑下划线** |
| **折叠状态感知** | 易误判为按钮或外链 | 表达明确，但线条过长产生表格感 | **清晰易读，保留交互暗示，完全无按钮感** |
| **解答正文呈现** | 带有左侧虚线框与缩进，像评论块 | 纯文本连续流，无侧边虚线 | **纯文本连续流，无侧边虚线，段尾 QED ∎** |
| **信息密度与连续性** | 差，版面碎片化严重 | 良好，但横向线条过于密集 | **极佳，信息密度高，阅读视线自然流畅** |
| **大学教材相似度** | 低（教育 App 风格） | 中（偏技术文档） | **极高（纯正大学数理教材纸质版质感）** |

---

## 六、最终采纳方案（方案 C 极简语义体系）

经过 A/B/C 实测，方案 C 在「学术严肃性」、「信息密度」与「交互直觉」之间达成了最佳平衡：
- **例题与定理头部**：以 SemiBold 650 字重配合 1px 低饱和发丝线构建顶层结构；
- **解与证明交互**：以 `解 ›`（展开后为 `解 ⌄`）内联发丝线下划线承载，保持数字阅读器的即时折叠能力，但消除了任何按钮式 UI 侵入；
- **排版节奏**：消除所有伪卡片边框与非必要内边距，公式、图表、正文完全自洽于版心排版网格中。

---

## 七、验证结果

1. **生产构建验证**：
   - 运行全站完整构建：`npm run build`
   - 全书 6 本教材、595 个 HTML 页面全部成功编译，Exit Code: 0。
2. **多端与视口验证**：
   - **3.4 定积分的应用**：例 4.1、4.2、4.3 折叠态与展开态均完美呈现，插图（图 3.11、3.12、3.13）与数学公式排版无错位。
   - **1.2 数列的极限**：定义 2.1、定理 2.1 及反证法推导展开折叠顺畅，公式与标号（4.1）对齐准确。
   - **Desktop 视口 (1280×900)**：三栏排版稳定，右侧 TOC 锚点跳转精准定位。
   - **Mobile 视口 (390×844)**：消除水平横向溢出，紧凑垂向节奏大幅提升移动端阅读舒适度。
   - **Dark 护眼夜读模式**：发丝线与文字灰阶在暗黑底色下自动适配为低噪护眼调和色，无刺眼高光。

---

## 八、Remaining Gaps

1. **部分历史 OCR 图片中的图例编号排版**：少数旧章节中插图下方存在原始文本残留的图编号，建议在后续内容精修中进一步利用 `<figure>` 统一清洗。
2. **极少数超长行内公式移动端换行**：个别未拆分的密集长积分式在极窄屏（< 360px）下仍依赖横向轻微滚动，已由全局 overflow-x 防御接管。

---

## 九、Out of Scope（超出本次边界）

1. 页面三栏整体布局（左侧目录、主阅读区、右侧大纲宽度）保持完全不变。
2. 阅读器版心宽度保持 48rem（768px）规范不变。
3. MDX 文件源码数据结构、Frontmatter 及 Markdown 文本未作任何修改。
4. 全站业务功能（跨页引用、AI 问答、公式复制、LaTeX 导出、习题系统）保持原样运行，不受任何影响。

---

> **最终明确说明**：
> 本次修改仅针对 Academic Semantic Blocks 的视觉层级与交互呈现，没有修改页面 Layout、数据结构、业务逻辑及既有交互机制。
